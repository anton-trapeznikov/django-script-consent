Готовим версию 0.3.0

В корне проекта есть файл design.png, там примерно новый вид банера показан.
Не важны шрифты, цвета, оформление чекбокса и их расположение, -- это пример дизайна для конкретного заказчика. Важен функционал банера. Дефолтное оформление пока не меняем.

В новый релиз должны войти следующие изменения.

1. В модель ScriptSnippet добавляем поле "Получатель" (по английски или, не знаю, как лучше назвать поле, чтобы смысл сохранить). По дефолту в поле пусто. Если у скриптов категории заполнен хотя бы один получатель, то добавляем в банер инфу по нему для категории (см. дизайн).

2. В настройку банера добавляем поле "оператор" (см. дизайн). По умолчанию пусто. Если поле заполнено, то добавляем инфу как в дизайне. Если же нет, то только ссылка на политику.

3. Добавляем явную информацию как обрабатывается закрытие окна без согласия (см. дизайн).

4. Кнопка "принять все" сейчас выделена отдельно от других, это нарушает 152 ФЗ. Убираем с нее акцент и добавляем его "Принять выбранные".

5. Добавляем каждому банеру статистику (каждая метрика в отдельном поле и есть в админке):
5.1 Всего показов банера
5.2 Явных закрытий
5.3 Только необходимых
5.4 Сохранений выбранных
5.5 Принято все.

Работай сразу в ветке, но не коммить.
Не используй локальные импорты.
Пиши по-русски.

Но сначала давай обсудим. Насколько это изменение полезно, с твоей точки зрения?